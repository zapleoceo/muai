[project]
name = "vera-ingestor-telegram"
version = "0.3.0"
requires-python = ">=3.12"
dependencies = ["vera-shared", "telethon>=1.36"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/ingestor_telegram"]
