[project]
name = "vera-brain-search"
version = "0.3.0"
requires-python = ">=3.12"
dependencies = ["vera-shared", "fastapi>=0.110", "uvicorn[standard]>=0.27"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/brain_search"]
