[project]
name = "vera-brain-triage"
version = "0.3.0"
requires-python = ">=3.12"
dependencies = [
    "vera-shared",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/brain_triage"]
