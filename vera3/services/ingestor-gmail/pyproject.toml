[project]
name = "vera-ingestor-gmail"
version = "0.3.0"
requires-python = ">=3.12"
dependencies = ["vera-shared", "httpx>=0.27"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/ingestor_gmail"]
